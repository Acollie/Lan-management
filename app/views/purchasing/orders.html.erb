<h1>Orders to be paid</h1>
<table>
  <thead>
  <tr>
    <th>User</th>
    <th>Due amount</th>
    <th>Order made at</th>
    <th>Items</th>
    <th></th>
  </tr>
  </thead>
  <tbody>
  <% @orders.where(paid:false).each do |order| %>
    <tr>
      <td><%= order.member.first_name %> <%= order.member.surname %></td>
      <td><%= order.total_price %></td>
      <td><%= order.created_at %></td>
      <td>
        <% order.item_orders.each do |item| %>
          (<%= item.admin_item.name %>
          <%= item.quantity %>
          £<%= item.price %>)
        <% end %>

      </td>
      <td><%= link_to 'Paid',purchasing_paid_path(order),class: "button",method: "post" %></td>
    </tr>
  <% end %>
  </tbody>
</table>


<h2>Orders already confirmed</h2>
<h1>Orders to paid</h1>
<table>
  <thead>
  <tr>
    <th>User</th>
    <th>Paid</th>
    <th>Order made at</th>
    <th></th>
  </tr>
  </thead>
  <tbody>
  <% @orders.where(paid:true).each do |order| %>
    <tr>
      <td><%= order.member.first_name %> <%= order.member.surname %></td>
      <td><%= order.total_price %></td>
      <td><%= order.created_at %></td>
      <td></td>
    </tr>
  <% end %>
  </tbody>
</table>